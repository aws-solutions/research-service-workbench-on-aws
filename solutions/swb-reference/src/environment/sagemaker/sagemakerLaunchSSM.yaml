description: Sample SSM document to execute a lambda function
schemaVersion: '0.3'
parameters:
  userName:
    type: String
mainSteps:
  - name: executeLambda
    action: 'aws:invokeLambdaFunction'
    description: Execute hello world lambda
    inputs:
      FunctionName: testHello
      Payload: |-
        {
          "key1": "{{userName}}"
        }
