const envKeyNameToKey: { [key: string]: string } = {
  environment: 'ENV',
  project: 'PROJ',
  account: 'ACC',
  instance: 'INID',
  envType: 'ET',
  envTypeConfig: 'ETC',
  dataset: 'DS'
};

export default envKeyNameToKey;
